<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        #message{
            color: red;
        }
    </style>
</head>
<body>
    <form action="/verifyEmail" method="post">
        <span id="message"></span>
        OTP Sent to <b><span id="email"></span></b><br><br>
        <input type="text" placeholder="Enter OTP" name="otp">
        <input type="submit" value="Submit">
    </form>

    <script>
        let Message = '<%- (Message) %>';
        console.log(typeof(Message),"Wrong OTP",Message == 'Wrong OTP');

        if(Message == 'Wrong OTP')
        {
            document.getElementById('message').textContent = "Enter Correct OTP";
        }
        else{
            document.getElementById('message').textContent = "";
        }
        document.getElementById('email').textContent = '<%- JSON.stringify(Email) %>';
        console.log('<%- JSON.stringify(Email) %>');
    </script>
</body>
</html>